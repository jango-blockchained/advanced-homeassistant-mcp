---
/**
 * CardContent — main body of a card.
 */
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";
const { class: className, ...rest } = Astro.props as HTMLAttributes<"div">;
---
<div class={cn("p-6 pt-0", className)} {...rest}>
  <slot />
</div>
