---
/**
 * CardTitle — h3 styled for card headers.
 */
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";
const { class: className, ...rest } = Astro.props as HTMLAttributes<"h3">;
---
<h3
  class={cn(
    "text-2xl font-semibold leading-none tracking-tight",
    className,
  )}
  {...rest}
>
  <slot />
</h3>
